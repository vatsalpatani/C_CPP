void main()
{
	int i,max,min,n;
	clrscr();
	printf("enter n..\n");
	for(i=1;n!=0;i++)
	{
		scanf("%d",&n);
		if(i==1)
		min=max=n;
		else if(max<n)
		max=n;
		else if(n<min  && n!=0)
		min=n;
	}
	printf("\nmin = %d\nmax = %d",min,max);
	getch();
}